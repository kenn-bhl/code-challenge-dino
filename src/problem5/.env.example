PORT=3000
# PostgreSQL connection string from your environment (host, SSL, credentials as required).
# DATABASE_URL=postgres://USER:PASS@db.example.com:5432/mydb?sslmode=require
DATABASE_URL=

# When set, all /api/resources/* requests must send this key (X-API-Key or Authorization: Bearer).
API_KEY=your-secret-key
